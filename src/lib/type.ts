/**
 * The big page heading ("About.", "Tell me what to build.", "Hit send."): Sora
 * thin. One shared class string so the About and contact pages stay the same size.
 */
export const PAGE_HEADING =
  'font-headline text-4xl leading-[1.05] font-extralight tracking-[-0.02em] wrap-break-word sm:text-6xl lg:text-[clamp(2.5rem,3.7vw,3.75rem)]';

/** Small mono label above a block ("What I build", "Skills", "Got a project?"). */
export const EYEBROW =
  'font-mono text-[11px] tracking-[0.03em] text-zinc-600 dark:text-zinc-400';

/** Keyboard focus ring for glass pill buttons. */
export const PILL_FOCUS =
  'focus-visible:outline-2 focus-visible:outline-offset-3 focus-visible:outline-black/30 dark:focus-visible:outline-white/40';
